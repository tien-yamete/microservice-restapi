server:
  port: 8085                  # Port server ch?y
  servlet:
    context-path: /order   # Context path chung cho API

spring:
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/order_service}
    username: ${DBMS_USERNAME:root}
    password: ${DBMS_PASSWORD:814362}
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true       # Log SQL ra console
  kafka:
    bootstrap-servers: localhost:9094
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false